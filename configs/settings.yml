environments:
  default:
    domain: montblu.eu
    name: staging
    profile_name: mb-staging
    region: eu-west-1
  permanent:
    - staging
general:
  organization: mb
  secrets_key_id: alias/secrets
sites:
  ordered:
    - inception
    - network
