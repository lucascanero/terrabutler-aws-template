environments:
  default:
    domain: montblu.eu
    name: staging
    profile_name: mb-staging
    region: eu-central-1
  permanent:
    - staging
  temporary:
    secrets:
      firebase_credentials: DUMMY
      mail_password: DUMMY
general:
  organization: mb
  secrets_key_id: alias/secrets
sites:
  ordered:
    - inception
    - network
