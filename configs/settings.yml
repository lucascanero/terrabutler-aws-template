environments:
  default:
    domain: domain_var
    name: env_var
    profile_name: project_var-env_var
    region: eu-central-1
  permanent:
    - env_var
  temporary:
    secrets:
      firebase_credentials: DUMMY
      mail_password: DUMMY
general:
  organization: project_var-org
  secrets_key_id: alias/secrets
sites:
  ordered:
    - inception
    - network
