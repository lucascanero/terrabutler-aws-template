environments:
  default:
    domain: domain_var
    name: env_var
    profile_name: organization-env_var
    region: eu-central-1
  permanent:
    - env_var
  temporary:
    secrets:
      firebase_credentials: DUMMY
      mail_password: DUMMY
general:
  organization: organization_var-org
  secrets_key_id: alias/secrets
sites:
  ordered:
    - inception
    - network
